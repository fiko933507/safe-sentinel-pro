name: Run Field Hardening (retired)

# This workflow was used for the one-time production hardening pass and is now
# intentionally inert. Keeping the file as documentation avoids stale workflow
# failures while preserving the audit trail.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired workflow notice
        run: echo "Field hardening has been completed and merged into main."
